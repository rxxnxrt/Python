#Solicite ao usuario suas notas. Se for digitado um valor menor que 0 o programada deve parar.
#Apos ser digitas todas as notas calcule a média de todas as notas!


contador_notas = 0
contador = 0
media = 0

while True:
    nota = int(input("Digite suas notas: "))
    if nota < 0:
        break
    else:
        contador_notas += nota
        contador += 1

if contador > 0:
    media = contador_notas / contador
    print(f"A media dos números digitados foi: {media}")
else:
    print("Nenhuma nota valida foi digitada.")